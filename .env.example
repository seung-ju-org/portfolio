DATABASE_URL="postgresql://postgres:postgres@localhost:5432/portfolio?schema=public"

REDIS_HOST="127.0.0.1"
REDIS_PORT="6379"
REDIS_PASSWORD=""

SMTP_HOST="smtp.example.com"
SMTP_PORT="587"
SMTP_USER="smtp-user"
SMTP_PASS="smtp-password"
SMTP_DOMAIN="localhost"
SMTP_PROTOCOL="tls"
CONTACT_FROM_EMAIL="smtp-user@example.com"
CONTACT_TO_EMAIL="owner@example.com"

CONTACT_SIMULATE_EMAIL="false"

# Alternate key names (for environments that inject camelCase variables)
smtpHost="smtp.example.com"
smtpPort="587"
smtpDomain="portfolio.seung-ju.com"
smtpUser="smtp-user"
smtpPassword="smtp-password"
smtpProtocol="tls"
